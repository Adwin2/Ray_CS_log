- const int *   int const *
- int* const 
- int func() const {}       //only worked in a class

object  marked   const only could call    the  method  marked const

lambda  [=]pass value   [&] pass reference  capture option

initialize list  order should be same as the definition order

compiler  optimization

heap life time could be longer than stack  but more complicated

new ：allocate memory in the heap		not dynamic allocate memory

placement new  - order the initialiaze memory

-> `s special using
struct Vector {
 float a ,b ,c;
}

int main(){
int offset = (int)&((Vector3*)0)->x;    //0 could also be typed as nullptr
}


push_back --change to--> emplace_back

pre-compiled binaries should be match to your project  e.g. X86 to 32-bit Windows (GLFW lib)


fir.add dll.lib  sec. put the .exe and the dll together  add DLL preprocessor

$solutionDirbin\$platform\$configuration



C++五大法则：
如果需要析构函数 则一定需要拷贝构造函数和拷贝赋值函数
移动构造函数 和 移动赋值函数（为了支持移动语义 后加）


summary :
helps a lot on C++17 features
